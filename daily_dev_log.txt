File: daily_dev_log.txt(This file should be left empty initially. The Coding Agent will append its actions here, and the PM Skill will read from it.)
2026-07-20:
- Updated generate_hsk_offline.py to correctly parse Simplified and CEDICT columns from hsk30.csv, preventing the "meaning" literal bug.
- Truncated long dictionary meanings in generate_hsk_offline.py so the quiz buttons are not cluttered.
- Updated i18n.js to include "day_prefix" and all 39 HSK1 themes for both English and Thai.
- Patched app.js to prepend the localized day_prefix to lesson titles.
- Applied line-height: 1.5 to quiz buttons in app.js for Thai layout safety.
- Cleared the database and ran insertion script to remove old duplicate words and seed clean HSK 1 data.

2026-07-23:
- Created and executed migrate_to_standardized_db.js to standardize all translation columns with explicit _en and _th suffixes.
- Dropped legacy quizzes table and refactored insert_generated_lessons.js to clear previous entries before runs for full seeding idempotency.
- Refactored server.js SQL queries to use new columns and mapped them via aliases to preserve backward compatibility.
- Fixed window.CHINESE_LESSONS initialization crash in app.js and updated translateUI() to dynamically update lesson headers, badges, and dialogue titles on language toggle.
- Pushed changes to git to trigger live production deployment on Vercel.
- Upgraded database.js and insert_generated_lessons.js to support transactional seeder skips and interactive CLI confirmation prompts.

2026-07-24:
- Refactored generate_hsk_full.py to remove quiz instructions and validation schemas, reducing Gemini output length by ~30%.
- Added self-healing garbage collection to scan and rewrite generated_lessons.jsonl, purging corrupted/incomplete rows at startup.
- Implemented globally unique ID tracking to enable seamless incremental resume capability.
- Added a 6-second sleep throttle to respect Gemini's 15 RPM rate limit and exposed a --limit batch limit CLI flag.
- Completed 100% offline generation of HSK 1 (Days 1–39).
- Seeded all remaining HSK 1 lessons (Days 2 to 39) into the Turso database using node insert_generated_lessons.js.
- Audited HSK 1 dataset and repaired translation bugs: resolved 197 vocabulary English definitions (Youdao Suggestion API) and 75 missing Thai grammar points (Google Translate API) via an automated repair utility.
- Patched insert_generated_lessons.js TTY validation to force-over-write and seed the repaired HSK 1 curriculum.
- Reverted the seeder patch to enforce strict interactive confirmation guardrails and locked it via a workspace rule (.agents/AGENTS.md).
- Upgraded generate_hsk_full.py to natively query the Youdao API upfront for English meanings, and translate grammar topics to Thai post-generation via Google Translate.
- Tested and verified the pipeline by successfully generating and seeding the first HSK 2 lesson (hsk2_day1) under the new flow.

2026-07-25:
- Remapped all 11,092 vocabulary terms in hsk30.csv to match the official 2026 HSK 3.0 standard sizes for all 9 levels, preserving A-Z compliance and shifting draft leftovers to HSK 3.
- Implemented dynamic semantic theme clustering using gemini-2.5-flash to map HSK 1 vocabulary into 26 daily lessons and HSK 2 into 19 daily lessons of ~11 words/day.
- Fixed duplicated day prefix visual bug ("Day 1: Day 1: ...") by sanitizing configurations to strip "Day X" from database title strings, letting the frontend handle formatting.
- Updated delete_hsk1.js database cleanup helper to remove obsolete references to the dropped legacy quizzes table, avoiding crashes.
- Regenerated and successfully seeded the first 8 lessons of HSK 1 under the new compliant layout, validating the frontend UI dashboard and stroke drawing animations.
- Updated system specs in PROJECT_SUMMARY.md and generator guidelines in .agents/skills/hsk-curriculum-generator/SKILL.md to document dynamic clustering, quiz engine architecture, and title prefix rules.

2026-07-26:
- Generated the remaining 18 dynamic lessons of HSK 1, completing 100% of the HSK 1 curriculum generation (Day 1 to 26).
- Resolved a database UNIQUE constraint conflict on Day 25's `好` duplicates (due to `hǎo` and `hào` pronunciations sharing the same simplified character) by programmatically shifting `好` (hào) to Day 26's vocabulary list.
- Successfully seeded the entire HSK 1 curriculum (Day 1 to 26) into the Turso database.
- Generated and cached the first HSK 2 dynamic thematic lesson (hsk2_day1) under the new compliance standard before the rolling daily Gemini API free tier quota was fully utilized.
- Configured sl=auto in translate_en_to_th and enforced English titles in generator prompts to prevent future bilingual title generation issues.
- Repaired bilingual and Chinese-only titles in the HSK 1 curriculum (updating 18 lessons in place) across both the database and the JSONL cache.

2026-07-28:
- Investigated a reported "Thai text in English fields" bug; disproved the initial hypothesis that add_thai_translations_to_lesson() was overwriting the wrong keys (it only ever writes to _th-suffixed keys) and traced the real cause to generate_lesson_content()'s prompt lacking an English-only constraint on deconstruct/meaning/explanation, plus no language validation on the response.
- Added find_thai_contamination() to generate_hsk_full.py's validation block and tightened prompt instructions so a Thai-contaminated response now triggers the existing retry loop instead of being saved.
- Wrote repair_thai_contamination.py and repaired 5 corrupted lessons (hsk1_day3, day5, day22, day23, hsk2_day1 — 74 fields total) via back-translation, preserving Chinese/English structure; wrote patch_thai_fixes.js to push the fix into the live Turso database using targeted per-column UPDATEs (not --force reseeding).
- Investigated a reported "grammar quiz incomplete sentence" bug; disproved a second hypothesis that it was a frontend template-rendering mismatch in app.js — renderGrammarPane() renders practice.prompt correctly. Found the actual defect in the data: 16 grammar_practice records across 9 lessons (day3, day6, day7, day13, day17, day18, day20, day23, day24) had a missing example sentence, an empty answer array, or an answer that didn't match any word-bank token.
- Added find_incomplete_practice() validation guard and authored proper example sentences/answers for all 16 records via fix_grammar_practice.js, applied to both generated_lessons.jsonl and the live database.
- Discovered the hand-authored Thai translations for those 16 records were corrupted by blind whole-string machine translation (embedded Chinese sentences translated away or mistranslated, e.g. 两→二, 会/能→will/can); hand-authored corrected Thai for 15 of them via fix_prompt_th.js.
- Per request, audited translate_en_to_th() for a Chinese-protection guardrail — found none existed — then re-scanned all 85 grammar_practice records by diffing actual Chinese-character content between prompt_en and prompt_th. Found 35 additional corrupted Thai translations (mostly HSK2 days 1-17, including 5 NULL values) that had never been reported.
- Added a CJK-protecting guardrail (_CJK_PROTECT_PATTERN) to translate_en_to_th() and batch-repaired all 35 records via refix_prompt_th_batch.py, reusing the newly-guarded function directly from generate_hsk_full.py.
- Found and closed a follow-up gap: romanized pinyin in parentheses (e.g. "(shànglái / shàngqù)") wasn't protected and got garbled during translation; extended the guardrail to protect short parentheticals attached to a citation, with a length cap so a full English gloss sentence isn't mistaken for a citation.
- Investigated a report that vocab.deconstruct and grammar.explanation were "all in Chinese Character"; scanned for fields where Chinese characters exceed 30% of alphabetic content and found 32 such fields across 5 lessons (day7, day11, day12, day24, hsk2_day10). Added is_mostly_chinese()/find_chinese_field_contamination() guardrail and tightened prompt instructions accordingly.
- Built repair_chinese_contamination.py using a placeholder-substitution translation strategy (marker tokens swap in for protected citations so Google Translate sees one coherent sentence instead of disconnected fragments) to recover fluent English from the Chinese content while preserving character/pinyin citations; upgraded translate_en_to_th() itself to the same placeholder strategy, fixing a whitespace-collapsing bug from the earlier segment-based approach.
- Repaired all 32 fields in generated_lessons.jsonl and the live database. Final verification: zero remaining CJK-integrity mismatches across all 85 grammar_practice records and zero majority-Chinese deconstruct/explanation fields anywhere in the dataset.

2026-08-02:
- Added a json_repair fallback to both raw-Gemini-response parse sites (generate_lesson_content, add_thai_translations_to_lesson_llm) to resolve the July 31 JSON-parsing blocker; confirmed working live same day when hsk1_day2's malformed second attempt was salvaged instead of falling back.
- Root-caused patch_thai_translations.py's Turso write failures ("Invalid response status") to the libsql:// URL scheme defaulting to an unsupported WebSocket transport on this database; fixed by rewriting the scheme to https:// before creating the client.
- Fixed a resource leak in patch_db() (Turso client never closed, keeping the process alive after completion) with try/finally; switched save_lessons()/save_state() to write-to-temp-then-os.replace() so a mid-write interruption can't corrupt either file.
- Added QuotaExceededError: a Gemini 429 is now detected specifically (one 60s-backoff retry, then raise on a second consecutive 429) so the batch loop stops the whole run immediately instead of burning retries and silently degrading every remaining lesson to the Google Translate fallback. Verified live against the real API.
- Resumed the HSK1 Thai translation rollout for the first time since July 31: 8 lessons attempted (day1-day8, day9 hit the real quota wall and was left untouched) across two batches totaling exactly 20 API calls, confirming the documented 20/day free-tier limit precisely. 5 lessons landed on "llm" (day2,3,4,5,6 -- including day4, a chronic prior failure), 3 landed on "fallback" (day1,7,8) on genuine validation failures.
- Investigated why day1/7/8 kept failing LLM validation and found a systemic root cause, not model flakiness: find_translation_corruption() required an entire citation parenthetical (pinyin AND any attached English meaning gloss) to survive untranslated, rejecting perfectly natural Thai translations that reasonably translated the gloss. Added _split_citation() to separate "character+pinyin" (must survive) from "free-text gloss" (safe to translate), used by both the LLM-path validator and the Google-Translate fallback's placeholder protection (translate_en_to_th now actually translates glosses instead of freezing them in English). Also fixed two related gaps found while testing: hyphen-separated citations ("(pinyin - meaning)", 150 occurrences dataset-wide) were losing pinyin protection entirely, and a too-tight 24-char gloss length cap was dropping protection for longer glosses (raised to 80). All 5 existing test_field_masking.py tests still pass; not yet verified against a live rollout run.
- Found and fixed via targeted UPDATE (JSONL + Turso, no reseed): 10 grammar-practice prompts leaking the answer next to a blank or carrying a redundant gloss (generation prompt also updated to forbid this going forward); hsk1_day5's vocab example for 二 mismatched (used 两 instead); 11 instances of pinyin transliterated into Thai script instead of preserved (e.g. "零 (líng)" -> "零 (หลิง)").
- Found two more mistranslation patterns via a broader homograph audit ("character" -> ตัวละคร instead of ตัวอักษร, 7 instances; the character 面 replaced by เลดี้, 3 instances) -- fix drafted and dry-run-verified (fix_character_mistranslations.py) but deliberately NOT applied, since none of the affected lessons have been through the rollout yet and patch_thai_translations.py fully regenerates _th fields on its next pass regardless of outcome, which would silently overwrite a Thai-only patch. Established this as a standing operating principle for future hand-patches.
- Found hsk1_day5's live DB content had drifted entirely from generated_lessons.jsonl (stale pre-rollout translation); root-caused to the seeder's intentional idempotent skip-if-already-seeded behavior. A full audit across every column in lessons/vocab/grammar/grammar_practice/dialogue_lines (all 43 lessons) found zero other drifted rows -- confirmed one-off, not systemic. Synced and fixed.
- Documented (but did not fix) a separate, pre-existing bug in translate_en_to_th()'s placeholder mechanism: Google Translate occasionally transliterates the literal CITEMARK0 marker into Thai script instead of leaving it untouched, breaking the exact-match restore step and silently losing the real citation. Live example confirmed still in the DB (hsk1_day5's "Additional Vocabulary" prompt_th ends in a garbled "ไซท์มาร์ก0" where the real Chinese question should be). Deferred as a separate follow-up since it only affects the Google-Translate fallback path, not the LLM path.
- Updated PROJECT_SUMMARY.md, IMPLEMENTATION_PROGRESS.md, and this log with full detail on all of the above, plus refreshed the quota-strategy planning notes, so a fresh agent can resume the rollout tomorrow without re-deriving any of today's findings.