# HanPath Project Summary
**Date:** August 7, 2026 (Last Updated)

## 1. Business Requirements

**Product Vision:** 
HanPath is a structured, daily Chinese learning application tailored to make language acquisition engaging, consistent, and accessible. It is designed with a kid-friendly, gamified aesthetic while retaining robust educational structures suited for all ages. 

**Core Features & User Flows:**
- **Curriculum & Pacing:** Content is mapped to standard HSK proficiency levels (HSK 1, 2, and 3). The curriculum is delivered in daily, digestible 1-hour sessions to prevent burnout and encourage habit-building.
- **Visual 5-Tab Pinyin Suite (Lesson 0 Foundation):**
  - *Tab 1 (📊 Syllable Matrix):* Interactive 1,600+ audio grid mapping initials, finals, and four tones.
  - *Tab 2 (👄 Mouth & Airflow Guide):* 6 anatomical cut-away SVG profile diagrams and physical guides covering Retroflex (`zh, ch, sh, r`), Palatal (`j, q, x`), Dental (`z, c, s`), Tissue Paper Test 💨 (Aspirated vs. Unaspirated), 'ü' Vowel Sound & Mouth Position, and Front Nasal (`-n`) vs Back Nasal (`-ng`) Airflow resonance contrast.
  - *Tab 3 (🎵 Tone & Phonetic Rules):* 13 interactive rule cards covering Consonants & Basic Vowels, Four Tones & Neutral Tone, 3rd Tone Sandhi (`3+3 ➔ 2+3`), Half 3rd Tone Sandhi (`3+1/2/4 ➔ Low Dip 211`), Tone Changes for 'Yi' and 'Bu', 'ü' Dot Removal Rule (`j, q, x, y`), 'e' Sound Mutation (`ie/üe ➔ eh`), 'a' Sound Mutation (`ian/üan ➔ eh`), Vowel Contractions (`iou➔iu`, `uei➔ui`, `uen➔un`), Tone Mark Placement Priority (`a > o > e > i / u`), Erhua (`-r`), Syllable Separator Apostrophe (`'`), and Capitalization & Word Separation Rules (`正词法`).
  - *Tab 4 (🎧 Tone Pairs Practice):* 20 interactive cards covering 100% of 2-tone combinations (1+1, 1+2, ..., 4+4, 4+0) using real HSK 1 vocabulary (`今天`, `明天`, `你好`, `再见`).
  - *Tab 5 (⌨️ Pinyin Typing Game & IME Simulator):* Zero-Initials & IME typing rules + pedagogically sound challenge loop: presents a meaning/audio prompt (target Hanzi hidden `❓ ❓`), learner types Pinyin, IME candidate choices appear, and correct selection reveals the Hanzi + plays native audio.
- **4-Stage Daily Learning Engine:**
  - *Stage 1 (Vocabulary):* Interactive flashcards, stroke tracing (via HanziWriter), and pronunciation.
  - *Stage 2 (Grammar):* Concept explanations, examples, and sentence-reordering exercises.
  - *Stage 3 (Dialogue):* Conversational practice with Text-To-Speech (TTS) playback and bilingual translations.
  - *Stage 4 (Quiz):* Comprehensive end-of-lesson assessment to verify retention.
- **Diagnostic Pre-Testing:**
  - *Placement Test:* A global diagnostic tool to determine a user's starting HSK level.
  - *Lesson Pre-Test:* A micro-assessment allowing advanced users to "test out" of lessons they already know.
- **Retention & Gamification:** Daily streak counters, total time tracked, lesson completion scores, and user-configurable daily study reminders.
- **Vocab Garden (Plants & Watering SRS - Revamped):** Now the central Home Screen and Dashboard of the application. Active vocabulary memory is represented as a living plant garden across 4 visual growth stages: 🌱 **Seeds** (Day 1) ➔ 🌿 **Sprouts** (3 Days) ➔ 🌻 **Flowers** (7 Days) ➔ 🌳 **Golden Trees** (21+ Days). Features a dynamic **Daily Memory Habit Banner** with a single-click Watering CTA, a **Total Vocab Progress Bar**, a **Confusion Pairs Alert Banner** for confusing lookalike characters (e.g., 买 vs 卖, 喝 vs 渴) currently in the garden, a **Compact Lessons & Quests Sidebar** with collapsible completed items, a full-screen **Watering Review Arena (`#srs-view`)** running SM-2 queues, and two new active mini-games: **🧪 Seed Fusion Lab** (to combine single characters into HSK compound words) and **🏹 Sentence Quest Builder** (drag-and-drop sentence ordering for mastered Stage 4 words).
- **Bilingual Support:** Full UI and content localization in both English and Thai.

---

## 2. Technical Specification

**Architecture Overview:**
HanPath operates as a Single Page Application (SPA) with a lightweight Node.js backend connected to a remote cloud database.

**Frontend (Client-Side):**
- **Tech Stack:** Vanilla HTML5, CSS3 (Custom utility classes, CSS Variables, Glassmorphism design), and Vanilla JavaScript (ES6+). Loaded as native ES Modules.
- **Modular Architecture (Vocab Garden Upgrade):** Decoupled the monolithic client-side `app.js` into clean, single-responsibility ES modules synchronized via a lightweight Pub/Sub `event-bus.js`:
  - `modules/srs-engine.js`: Manages SM-2 interval calculations and interleaved card practice in round-robin batches of 5.
  - `modules/challenge-selector.js`: Dynamically targets four different cognitive challenges based on a word's current Mastery Stage (Stage 1: recognition MCQ, Stage 2: tone ID/input, Stage 3: translation MCQ, Stage 4: drag-and-drop HSK sentence builder).
  - `modules/garden.js`: Renders the interactive plant canvas.
- **State Management & 0ms Memory Caching:** Centralized local JS `state` object synchronized with `localStorage` and backend. Lesson 0 reference payloads are cached in memory (`state.pinyinLessonData`) upon initial fetch, allowing instant **0ms language toggling** (TH ↔ EN) without network latency.
- **Localization & Language Purity Guardrail:** Custom robust i18n engine scanning DOM for `data-i18n` attributes. Pre-commit assertions (`validateLanguagePurity`) scan database seeding scripts to guarantee 0% mixed-language contamination in `_en` and `_th` fields.
- **Key Libraries & Audio Architecture:** `hanzi-writer.min.js` (for stroke animations and character drawing), Native Web Speech API with explicit `zh-CN` voice-object binding.
- **Self-Hosted Open Audio Assets & Hanzi Fallback:**
  - Core Pinyin audio assets (`/public/audio/pinyin/a1.mp3` - `a4.mp3`, `e1.mp3` - `e4.mp3`) are self-hosted directly within the project repository to eliminate third-party commercial CDN dependencies, hotlinking blocks, and 404 failovers.
  - An explicit fallback dictionary (`pinyinToHanziMap`) resolves ASCII Pinyin strings (`a1`, `zhi1`, `chi1`) to actual Chinese Hanzi characters (`啊`, `知`, `吃`), forcing browser SpeechSynthesis to activate native Mandarin voices instead of reading ASCII letters ("zhi-one").

**Backend (Server-Side):**
- **Tech Stack:** Node.js, Express.js.
- **Database:** Turso Cloud Database (SQLite distributed at the edge).
- **Driver:** `@libsql/client` wrapper with custom exponential backoff retry logic for resilience against network instability.
- **API Design & Static Serving:** RESTful endpoints. Includes detailed SRS integrations:
  - `GET /api/srs/garden`: Fetches statistics, progress bar, and the full list of planted cards (joined with `vocab` to retrieve Pinyin and translations for frontend rendering).
  - `POST /api/srs/fuse`: Receives two single characters, checks if they form a valid HSK compound word, and auto-plants the new compound card in the user's garden.
- **Static Asset Delivery:** Served via standard static paths.

**Database Schema Design:**
- **Standardized Column Layout:** All user-facing translatable strings are explicitly separated into English (`_en`) and Thai (`_th`) columns in the database (e.g., `meaning_en`, `meaning_th` in the `vocab` table; `explanation_en`, `explanation_th` in the `grammar` table). This provides standard translation schemas across the entire database.
- **Mixed-Language Character Preservation:** Shared linguistic attributes—specifically the original Chinese characters (`character` and `example_cn` columns) and their standard Pinyin pronunciations (`pinyin` and `example_py` columns)—are stored in unified, language-agnostic columns. Only the contextual explanations and translated meanings are split into English (`_en`) and Thai (`_th`) columns. This schema separation ensures that the core Chinese characters and pronunciations are never processed by translation engines, preventing character corruption and maintaining clean, mixed-language layouts.
- **Polyphonic Homograph Handling:** Changed unique key constraints on vocabulary table from `UNIQUE(lesson_id, character)` to `UNIQUE(lesson_id, character, pinyin)`. This supports polyphonic homographs (e.g. `干` pronounced as `gān` vs `gàn`) in the database.
- **SQL Translation Aliases:** The server routes fetch these language-specific fields and alias them (e.g. `meaning_en as meaning`) to maintain seamless backward compatibility with the frontend's legacy code.
- **Quiz Elimination from Database:** The legacy `quizzes` table has been completely dropped from the database schema because post-lesson quizzes are generated dynamically in the frontend (inside `app.js`) using the lesson's active vocabulary list. This guarantees that quizzes are always dynamically localized and are in 100% synchronization with vocabulary.

**Quiz Engine Handling (Three Quiz Types):**
The application implements three distinct quiz types, all handled dynamically in the client frontend (without database queries):
1. **Diagnostic Placement Test (Pre-Test):** A 10-question general assessment loaded dynamically from a static JSON array (`preTestQuestions`) in `app.js` upon signup. Scores are evaluated to place the student in their starting HSK level (Level 1, 2, or 3) and award initial points.
2. **Lesson-Level Pre-Test (Skip-Lesson Quiz):** A placement quiz triggered before starting a lesson. It dynamically compiles 5+ questions using the lesson's active vocabulary pool. If the user scores 100%, they skip the study phases, unlock the lesson, and get bonus points.
3. **Daily Post-Lesson Quiz (Retention Quiz):** Injected at Stage 4 of the study engine. It dynamically compiles a 10+ question practice quiz based on the daily lesson's vocabulary words. This ensures questions are always in sync with the vocabulary studied and are fully translated into Thai.

**Seeding & Idempotency Logic:**
- **Check-and-Skip Efficiency:** To prevent redundant writes and save Turso database operations, the seeder `insert_generated_lessons.js` checks the database before importing. It runs a single count query: `SELECT COUNT(*) as count FROM vocab WHERE lesson_id = ?`.
  - **Skip:** If the count is greater than `0`, it skips the lesson, avoiding unnecessary updates.
  - **Self-Healing:** If the lesson ID exists in `lessons` but the count is `0`, it automatically clears the remnant rows and re-seeds the lesson (handling past script crashes automatically).
- **Gatekeeper CLI Prompt:** If a force overwrite is required (passing the `--force` or `-f` flag), the script checks if the shell is interactive (`process.stdin.isTTY`). If interactive, it halts and demands a manual `yes/no` response before overwriting. If non-interactive (such as in an AI session or automated runner), it safely aborts to prevent accidental loss of data.
- **Interactive HTTP Transactions:** Standard SQL `BEGIN TRANSACTION` commands fail over stateless edge HTTP endpoints. To make seeding completely bulletproof, we exposed the native LibSQL `.transaction("write")` API in `database.js`. All operations for a single lesson are executed inside this transactional scope; if any insert fails, the transaction is rolled back, leaving zero corrupted or partial rows.

**Process for Future Database Adjustments:**
- **Structural Database Changes:** We will **never** perform destructive `DROP TABLE` operations on a live production database. Future schema modifications will be handled via additive SQL Migration Scripts using the `ALTER TABLE` statement (e.g., `ALTER TABLE vocab ADD COLUMN ...`).
- **Content Typo Fixes & Adjustments:** Small spelling corrections or translation fixes will be done using targeted `UPDATE` scripts (or in a future phase, a web-based Admin Panel) rather than re-running the entire seeder.
- **Seeding for New Curriculum:** The seeding script is treated as a **One-Way Seed**. It will only be run to import *new* curriculum levels (like HSK 2 and HSK 3) while existing levels remain permanently untouched.

**HSK Curriculum Generator Specification (Offline Pipeline):**
- **Source of Truth (Official 2026 Remap):** Word lists are read directly from `hsk30.csv`, which has been fully remapped to the official 2026 HSK 3.0 standard counts (Level 1 = 300 words, Level 2 = 200 words, Level 3 = 500 words, etc.).
- **Dynamic Theme Clustering:** The vocabulary is grouped into thematic days using an offline clustering script (e.g., `cluster_hsk_2026_themes_dynamic.py`). Instead of a forced Day count, the number of lessons emerges naturally based on the semantic grouping of the words (aiming for a density of ~8-12 words per lesson). This dynamic clustering is standard across all levels.
- **LLM-Based Content Generator:** Content is generated programmatically using `generate_hsk_full.py` powered by the `gemini-2.5-flash` model. It translates standard HSK words into high-quality vocabulary sheets, grammar pointers, and dialogue scenarios complete with English and Thai translations.
- **LLM-Based Contextual Translation Pipeline (Active):**
  - *Upfront English Meaning Lookup:* Integrates `get_youdao_meaning(word)` to fetch clean English definitions from the Youdao Suggestion API on-demand before construction of the Gemini payload, resolving placeholder bugs.
  - *LLM Contextual Thai Translation:* Replaced the previous stateless Google Translate approach with a **single Gemini API call per lesson** via `add_thai_translations_to_lesson(lesson_data)`. The entire lesson JSON (vocab, grammar, dialogue) is submitted in one prompt, giving the model full pedagogical context. The system prompt instructs it to produce natural, idiomatic, kid-friendly Thai — explaining grammatical particles by function rather than literal name. This eliminates the word-by-word translation bug that corrupted `deconstruct_th` and `explanation_th` fields.
  - *Quota-Aware Two-Pass Mode:* The generator supports a `--no-translate` flag to skip Thai translation during generation (saving ~50% of the daily API quota). A separate script `patch_thai_translations.py` can then re-run LLM translation as targeted `UPDATE` SQL statements against Turso without re-seeding English content.
  - *SDK:* Migrated from the deprecated `google-generativeai` package to the current `google-genai` SDK (`google.genai.Client`). Model confirmed: `gemini-3.5-flash` (20 req/day free tier).
- **Automatic Self-Healing Parser:** At start-up, the script parses `generated_lessons.jsonl` using a robust checker (`is_lesson_complete`) that scans for JSON schema completeness (ensuring presence of all core arrays, dialogue lines, and localized translation keys). Any incomplete or corrupt records are automatically purged, and the clean, sorted records are rewritten back to the file.
- **Seamless Incremental Resume:** Incorporates globally unique lesson IDs to query local `.jsonl` files and live database caches. Already generated days are skipped instantly, protecting against redundant API calls.
- **Throttling & API Budgeting:** Employs a 6-second sleep throttle to respect Gemini's 15 RPM limit and handles 429 rate limits gracefully with an automated sleep-and-retry strategy. It supports a `--limit` argument to generate a specific budget of lessons per run to prevent exceeding daily free tier budgets.
- **Data-Quality Validation Guardrails (July 28):** Added multi-layered post-generation validation to `generate_lesson_content()` to catch language-contamination defects before they ever reach the database:
  - `find_thai_contamination()` — flags English-only fields (`title`, `meaning`, `deconstruct`, `explanation`, dialogue title) that contain Thai script.
  - `find_incomplete_practice()` — flags grammar practice exercises missing an embedded example sentence, an empty `answer` array, or an `answer` containing a token absent from `words`.
  - `find_chinese_field_contamination()` — flags `deconstruct`/`explanation` fields where Chinese characters make up more than 30% of alphabetic content (i.e. the model answered in Chinese instead of English).
  - Any failed check raises a `ValueError`, which feeds the existing retry loop in `run_generation`, so a contaminated response triggers automatic regeneration instead of being saved.
- **CJK/Pinyin-Preserving Translation Guardrail (July 28):** Rewrote `translate_en_to_th()` to use placeholder substitution instead of naive whole-string translation: embedded Chinese citations (and their attached pinyin/gloss parentheticals, length-capped so a full English gloss sentence isn't mistaken for a citation) are swapped for a marker token before the string is sent to Google Translate, then restored verbatim afterward. This prevents Chinese sentences from being translated away, prevents pinyin like `shànglái` from being garbled into nonsense, and fixes a whitespace-collapsing/broken-grammar artifact from the previous segment-by-segment translation approach.

**Thai Translation Pipeline — Full Control Flow (as of Aug 2 — read this before touching `add_thai_translations_to_lesson_llm`):**
This is the function every rollout run (`patch_thai_translations.py`) and every fresh lesson generation ultimately calls to populate `_th` fields. It has an LLM-first, Google-Translate-fallback design with three distinct exit paths. All line numbers below refer to `generate_hsk_full.py` as of Aug 2.
1. **Setup:** `_strip_untranslatable_fields()` (line 235) deep-copies the lesson and deletes `character`/`pinyin`/`cn`/`py` before building the prompt — these are never read back by `_apply_translated_fields()` (line 259), so there's nothing for the LLM to corrupt even if it ignores instructions.
2. **Retry loop, up to `max_retries=3` attempts** (`add_thai_translations_to_lesson_llm`, line 287):
   - Each attempt calls `client.models.generate_content(...)` once (one Gemini API call = one unit of the 20/day free-tier quota).
   - The raw response is parsed with `json.loads()`; if that throws `JSONDecodeError`, it retries parsing with the `json_repair` library (added Aug 2) before giving up on this attempt — confirmed live to salvage responses `json.loads()` alone would reject (see Aug 2 log below).
   - A successfully-parsed response is validated by `find_translation_corruption()` (line 203), which checks array lengths match and that every citation extracted by `_extract_citations()` (line 194) survives in the corresponding `_th` field.
   - **On success:** fields are applied, `lesson_data["_th_source"] = "llm"`, function returns immediately. No more attempts are made.
   - **On a non-quota failure** (bad JSON even after repair, a corruption finding, a truncated response, a stray network error): prints the reason, sleeps 5s, and tries again — up to 3 total attempts.
   - **On an HTTP 429 from the Gemini API** (quota or rate-limit exhaustion, raised by the SDK as `google.genai.errors.APIError` with `.code == 429`): this is handled *differently* from other failures. The first 429 triggers a 60s backoff and one retry (to absorb a transient per-minute rate limit). If a **second consecutive 429** occurs, the function immediately raises `QuotaExceededError` (line 279) — it does **not** exhaust the remaining retry budget and does **not** fall back to Google Translate.
3. **Two ways the function can end:**
   - **Falls back to Google Translate:** if all 3 attempts are exhausted without ever hitting the quota-stop condition, it calls `add_thai_translations_to_lesson(lesson_data)` (the per-field Google-Translate translator, line 560) and marks `lesson_data["_th_source"] = "fallback"`.
   - **Raises `QuotaExceededError`:** propagates up to the caller (`patch_thai_translations.py`'s batch loop, around line 239) uncaught by this function. The caller catches it specifically, prints a message, and `break`s out of the whole batch immediately — **the current lesson's `_th` fields were already cleared in memory by the caller before this call, but since the batch loop never reaches its `save_lessons()`/`save_state()` calls for that lesson, nothing is written to disk.** The lesson is left exactly as it was before the run and stays eligible for a clean retry once quota resets. This was verified with a live quota exhaustion on Aug 2 (see below) — the interrupted lesson's on-disk JSONL entry was confirmed byte-for-byte unchanged.
   - Both `generated_lessons.jsonl` and `patched_lessons_state.json` are written via a temp-file-then-`os.replace()` pattern (added Aug 2), so even a hard interruption mid-write (crash, Ctrl-C) can't leave a truncated/corrupt file — the file is always either fully old or fully new.
4. **Once a lesson reaches `_th_source = "llm"`, it is locked forever:** `patch_thai_translations.py`'s batch loop filters lessons with `state.get(lid) != "llm"` (see `patched_lessons_state.json`), so an `"llm"`-tagged lesson is never reprocessed by future rollout runs. **A `"fallback"`-tagged or never-attempted lesson, by contrast, gets *all* of its `_th` fields cleared and fully regenerated from scratch the next time the rollout reaches it — regardless of whether that attempt succeeds via LLM or falls back to Google Translate again.** This has a critical, easy-to-miss consequence for anyone hand-patching Thai content (see "Operating principle" below).

✅ **CITEMARK Placeholder Mistranslation Fixed (Aug 3):**
`translate_en_to_th()`'s placeholder-substitution scheme previously used a literal marker string like `CITEMARK0`, `CITEMARK1`, ... which Google Translate occasionally transliterated into Thai script (e.g. `ไซท์มาร์ก0`). Fixed in `generate_hsk_full.py` on Aug 3:
1. Switched placeholder token format from `CITEMARK{n}` to code-structured `__CIT_{n}__`, which Google Translate treats as a non-word code symbol.
2. Pre-compiled `_RESTORATION_PATTERN` to match `__CIT_`, legacy `CITEMARK`, and Thai transliteration variants (`ไซท์มาร์ก`, `ไซต์มาร์ก`, `ไซมาร์ก`, `ซายท์มาร์ก`).
3. Added index bounds checking (`0 <= idx < len(citations)`) and safe network error fallback.
4. Added unit test suite in `test_field_masking.py` covering standard text, single citation, multi-digit citation, and legacy transliterated text restoration. All tests pass.

**Operating principle for hand-patching Thai content (established Aug 2 — read before writing any more `fix_*.py` scripts):**
Because of point 4 above, a targeted string-replacement patch to a `_th` field is only *permanent* if the lesson is already locked `"llm"` in `patched_lessons_state.json`. For any lesson still `"fallback"` or never-attempted, a Thai-only patch is temporary — it will be silently wiped and regenerated the next time the rollout reaches that lesson. Two categories of fix are exempt from this risk because they touch fields the rollout never clears: (a) fixes to the **English/Chinese source** (`deconstruct`, `explanation`, `example_sentence`, `practice.prompt`, etc. — never `_th`-suffixed), which persist forever and also improve whatever the *next* rollout attempt produces; (b) fixes to a lesson **already** `"llm"`-locked. Before patching a `_th`-only field on any other lesson, either (1) confirm the lesson is `"llm"`-locked first, or (2) accept the patch is a temporary stopgap and plan to re-audit after that lesson's next rollout pass.

**Data Schema (Core Entities & Standardized Columns):**
- `user_progress`: Tracks HSK level, scores, streaks, time spent, and completed lessons.
- `lessons`: Curricular structure mapping days to HSK levels. Columns: `id`, `hsk_level`, `day_number`, `title_en`, `title_th`.
- `vocab`: Relational vocab linking content to `lesson_id`. Columns: `id`, `lesson_id`, `character`, `pinyin`, `meaning_en`, `meaning_th`, `deconstruct_en`, `deconstruct_th`, `example_cn`, `example_py`, `example_en`, `example_th`.
- `grammar`: Grammar concepts linking content to `lesson_id`. Columns: `id`, `lesson_id`, `title_en`, `title_th`, `explanation_en`, `explanation_th`.
- `grammar_examples`: Examples linked to grammar rows. Columns: `id`, `grammar_id`, `cn`, `py`, `en`, `th`.
- `grammar_practice`: Practice questions linked to grammar rows. Columns: `id`, `grammar_id`, `prompt_en`, `prompt_th`, `words` (JSON array), `answer` (JSON array).
- `dialogues`: Dialogue headers linked to `lesson_id`. Columns: `id`, `lesson_id`, `title_en`, `title_th`.
- `dialogue_lines`: Individual conversation lines. Columns: `id`, `dialogue_id`, `speaker`, `cn`, `py`, `en`, `th`.
- `user_vocab_srs`: Spaced Repetition System parameters for Vocab Garden. Columns: `id`, `user_id`, `vocab_id`, `lesson_id`, `character`, `mastery_stage`, `ease_factor`, `interval_days`, `repetitions`, `next_review_date`, `last_reviewed_at`, `total_reviews`, `times_forgotten`, `created_at`. Indexed by `idx_user_srs_due`.

---

## 3. Project Progress

**Overall Completion Status: ~82%** (Core Application is ~100% Complete)

**Milestones Achieved:**
- ✅ **Phase 1: Database & Backend:** Schema setup, Turso cloud migration, API routes, and **Full HSK 1 curriculum seeding** are completed (26 days/300 words generated and seeded). **HSK 2 Days 1–18** generated and seeded (100% of HSK 1 and HSK 2 dynamic lessons generated and seeded into Turso).
- ✅ **Phase 2: Diagnostic System:** Global placement tests and per-lesson gating pre-tests are fully functional.
- ✅ **Phase 3: Core Learning Engine:** Vocab tracing, Grammar interactions, Dialogue UI, Pinyin Matrix chart, 20-Rule Pinyin orthography suite, and Quizzes are fully operational.
- ✅ **Phase 4: Consistency & Retention:** Streak tracking, progress syncing, daily study reminders, and **Vocab Garden Spaced Repetition System (SRS)** implemented.
- ✅ **Phase 5: UX & Localization:** Kid-friendly styling applied, inline styles refactored to CSS utility classes. Complete Thai translation injected across both the frontend UI and Turso database records (**100% of generated lessons locked on high-quality LLM Thai**).
- ✅ **Vocab Garden SRS Implementation & Production Release (Aug 8):** Designed, built, smoke-tested, and deployed a kid-friendly Spaced Repetition System featuring 4 visual plant stages (🌱 Seeds ➔ 🌿 Sprouts ➔ 🌻 Flowers ➔ 🌳 Golden Trees), Total Vocab Progress bar, UTC+7 date protection, historical backfilling, Wilting Rescue Box, and full-screen review arena (`#srs-view`).
- ✅ **Full HSK 1 & HSK 2 LLM Thai Rollout (Aug 8):** Completed LLM Thai translation rollout across all 44 generated lessons (HSK 1: 26/26, HSK 2: 18/18). All active lessons locked on contextual LLM Thai (`_th_source = "llm"`).
- ✅ **Curriculum Generator Overhaul (July 27):** Migrated generator from deprecated `google-generativeai` to new `google.genai` SDK; replaced per-field Google Translate with single LLM contextual translation call per lesson.
- ✅ **Data-Quality Guardrails & Corruption Repair (July 28):** Investigated two live-reported bugs — Thai text appearing in English fields, and incomplete/unanswerable grammar-quiz exercises — and traced both to gaps in generation-time validation rather than frontend rendering bugs. Added four validation guards to `generate_hsk_full.py` and repaired all discovered corruption in both `generated_lessons.jsonl` and the live Turso database via targeted `UPDATE` scripts (never full reseeds): 5 lessons / 74 fields of Thai contamination, 16 grammar-practice records with missing or structurally broken practice exercises, 35 grammar-practice Thai translations that had dropped or mistranslated their embedded Chinese sentences, and 32 `deconstruct`/`explanation` fields that were answered entirely in Chinese instead of English.
- ✅ **Field Masking for LLM JSON Structure Protection (July 29):** Discovered that hsk1_day4 validation failed because the LLM was modifying `cn`/`py`/`character`/`pinyin` fields in the JSON response despite explicit "NEVER modify" prompt instructions. Pattern-based protection (`_CJK_PROTECT_PATTERN`) proved ineffective at the JSON field level. Implemented field masking: rename untranslatable fields to `_*_masked` versions before sending to LLM, then unmask the response to restore original field names. Unit test suite (`test_field_masking.py`) validated the mask/unmask/validate pipeline — but only against a hand-written, perfectly-behaved fake LLM response. **Superseded July 31 — see below.**
- ✅ **Field Masking Replaced with Strip-Before-Send (July 31):** Live retry of hsk1_day4 with field masking still failed validation with the identical `vocab[你]: character/pinyin field altered` error masking was built to fix. Root-cause tracing found `_apply_translated_fields()` never reads `character`/`pinyin`/`cn`/`py` back from the LLM response at all — the round-trip comparison was validating something never consumed downstream, and renaming fields to unusual synthetic names (`_character_masked`) asked the LLM to preserve something *less* familiar than the original name, not more reliable. Replaced `_mask_untranslatable_fields()`/`_unmask_fields()` with a single `_strip_untranslatable_fields()` that deletes these fields from the LLM payload entirely (never re-added), removed the now-pointless equality checks from `find_translation_corruption()`, and rewrote `test_field_masking.py` with an adversarial fixture (a response that omits/alters these fields) proving validation no longer depends on the LLM echoing anything back. Logged as retrospective lesson #19 ("Validate Necessity Before Spending Live API Credits").
- ✅ **Windows Console Encoding Crash Fix (July 31):** The live hsk1_day4 retry (after the masking fix) hit a *second*, unrelated bug: `print()`-ing a validation error containing Chinese/Thai characters crashed with `UnicodeEncodeError` on Windows' default `cp1252` console encoding, aborting the retry loop after attempt 1 instead of retrying up to `max_retries` and falling back gracefully. Fixed by reconfiguring `sys.stdout`/`sys.stderr` to UTF-8 at the top of `generate_hsk_full.py`. This likely explains why prior retry attempts appeared to fail immediately rather than exhausting all 3 attempts.
- ✅ **JSON-Parsing Fragility Fixed with `json_repair` (Aug 2):** Added a `json.JSONDecodeError` fallback to both call sites that parse a raw Gemini response (`generate_lesson_content()` and `add_thai_translations_to_lesson_llm()`) — on a parse failure, retries with the third-party `json_repair` library before giving up on that attempt. **Confirmed working live**, not just in theory: during the Aug 2 HSK1 rollout batch, `hsk1_day2`'s first attempt hit `[WinError 10053]` then a malformed/truncated response on its second attempt, which `json_repair` successfully salvaged — the lesson landed on `_th_source = "llm"` instead of falling back. This closes out the July 31 blocker.
- ✅ **Turso Database Connectivity Fixed — Wrong Transport Protocol (Aug 2):** `patch_thai_translations.py`'s DB writes were failing with `400, message='Invalid response status'`. Root-caused to `TURSO_DATABASE_URL`'s `libsql://` scheme being interpreted by `libsql_client` as `wss://` (WebSocket-based Hrana transport) — confirmed via raw handshake test that this specific Turso database returns `{"error":"protocol upgrade not supported (websocket)"}` for that transport, regardless of subprotocol requested. Fixed by rewriting the scheme to `https://` before calling `create_client_sync()`, which routes through the HTTP-based Hrana transport instead. Verified live (a direct query succeeded immediately after the fix).
- ✅ **`patch_thai_translations.py` Resource Leak & Crash-Safety Fixes (Aug 2):** `patch_db()` created a Turso client per lesson but never called `.close()` on it (not even on the exception path) — the resulting unclosed websocket/HTTP client thread was keeping the Python process alive indefinitely after the script's own "Done." message printed, making it look hung when the actual work had already finished. Wrapped the DB operations in `try/finally` with `db.close()`. Additionally, `save_lessons()`/`save_state()` now write to a `.tmp` file and `os.replace()` it into place instead of writing the target file directly, so a mid-write interruption (crash, Ctrl-C, quota exhaustion) can never leave a truncated/corrupt `generated_lessons.jsonl` or `patched_lessons_state.json` on disk.
- ✅ **Quota-Exhaustion Handling Added — `QuotaExceededError` (Aug 2):** Previously, a Gemini 429 (quota/rate-limit) looked identical to any other failure inside `add_thai_translations_to_lesson_llm()`'s retry loop — it burned all 3 retries, then silently fell back to Google Translate and tagged the lesson `"fallback"`, exactly as if the content itself were the problem. If quota ran out mid-batch, every remaining lesson in that run would have gone through the same wasted-retries-then-degrade cycle. Added detection of `google.genai.errors.APIError` with `.code == 429` specifically: one 60s-backoff retry to absorb a transient rate limit, then on a second consecutive 429, raises `QuotaExceededError` immediately (no more retries, no fallback). `patch_thai_translations.py`'s batch loop catches this and stops the whole run before writing anything for that lesson. **Verified against the real API on Aug 2** — see the HSK1 rollout entry below; the interrupted lesson's on-disk JSONL was confirmed byte-for-byte unchanged afterward. See the new "Thai Translation Pipeline — Full Control Flow" subsection above for the complete mechanics.
- ✅ **HSK1 Thai Translation Rollout Resumed (Aug 2):** With the above fixes in place, ran the rollout live for the first time since the July 31 blocker. Result across 9 lessons attempted (`hsk1_day1` through `hsk1_day9`): 5 landed on `"llm"` (day2, 3, 4, 5, 6 — including `hsk1_day4`, previously a chronic failure per the July 31 entries), 3 landed on `"fallback"` after genuine (non-quota) validation failures (day1, 7, 8), and `hsk1_day9` hit the real quota wall (confirmed via Google's own error message: `limit: 20, model: gemini-3.5-flash`) and was left untouched for a future run. Tallying actual calls made across both today's batches landed at exactly 20 — confirming the documented daily limit precisely. 17 HSK1 lessons and all 17 HSK2 lessons remain unattempted; see Next Steps.
- ✅ **Over-Strict Citation Validation Fixed — Root Cause of Repeated Fallbacks (Aug 2):** Investigated *why* `hsk1_day1/7/8` kept failing LLM validation and found a systemic bug, not model flakiness: `find_translation_corruption()` required an *entire* citation parenthetical — pinyin **and** any attached free-text English gloss (e.g. `'亻' (person radical)`, `今 (now, present)`, `'禾' (huò, grain/harvest)`) — to survive byte-for-byte in the Thai translation. A natural, kid-friendly Thai translation reasonably translates "person radical" into Thai; the validator was rejecting that as "corruption" every time, forcing an avoidable fallback. Added `_split_citation()` (line 171), a shared helper used by **both** consumers of citation-matching: `_extract_citations()` (the validator) now only requires the character + pinyin (detected via tone-mark diacritics plus a fixed list of common toneless particles: `de, le, ne, ma, ba, a, ya, men, er`) to survive, never the gloss; `translate_en_to_th()` (the Google-Translate fallback) was also updated to protect only that same core from the placeholder swap, letting the gloss pass through to actual translation instead of staying frozen in English forever (a real quality improvement to the fallback path as a side effect). Also fixed two compounding gaps found during testing: the citation regex previously excluded hyphens (`[^-.!?)]`), silently dropping pinyin protection for the very common `"(pinyin - meaning)"` citation format (150 occurrences found dataset-wide) — hyphens are now allowed; and the parenthetical length cap (24 chars) was raised to 80 (based on the real max of 69 chars found in the dataset) since the actual "don't swallow a full sentence" protection now comes from `_split_citation()`'s pinyin-only requirement, not the character count. All 5 existing `test_field_masking.py` tests still pass; additionally verified against the real hsk1_day1/7/8 source text (natural translations now pass, genuine corruption is still caught) and a sample of the 150 hyphen-format citations. **Not yet re-run against the live rollout** — the next `patch_thai_translations.py` run will be the first live test of this fix; expect `hsk1_day1/7/8` to have a much better chance of landing on `"llm"` this time.
- ✅ **Content-Quality Bugs Found & Repaired via Targeted `UPDATE` (Aug 2)** — all via new local-only scripts `fix_practice_prompt_gloss.py`, `fix_vocab_er_example.py`, `fix_pinyin_transliteration.py` (untracked, `.gitignore`'d like the project's existing `fix_*.js` pattern), applied to both `generated_lessons.jsonl` and Turso:
  - **Answer-leaking / redundant glosses in grammar practice prompts (10 records)** — `_strip_practice_prompt_gloss()` broadened twice: first to strip a trailing whole-sentence English/Thai gloss after a Chinese fill-in-the-blank sentence (14 records fixed in an earlier pass), then to also strip a hint glued directly onto an individual blank marker (e.g. `你有 _____(สอง)_____(个) 铅笔吗？` — which literally handed the student both answers). The generation prompt was also updated to forbid this pattern going forward. Rationale: the drag-word UI already shows the answer pool, so no translation/hint belongs in the prompt text at all.
  - **`二`/`两` vocab example mismatch (`hsk1_day5`)** — the vocab entry for `二` (èr, "two") used the example `这里有两个人` (which uses `两`, not `二` — and `二` can't grammatically precede a measure word to count objects, so the example didn't demonstrate `二` at all). Replaced with `现在是二月` ("Now it's February"). A full-dataset audit (does every vocab's `example_sentence` contain its own character) confirmed this was the only instance.
  - **Pinyin transliterated into Thai script instead of preserved (11 instances across 7 lessons)** — e.g. `零 (líng)` → `零 (หลิง)`, `男 (nán)` → `男 (แนน)`. Root-caused to legacy content that predates any citation-protection guardrail (most of the dataset — see "Thai Translation Rollout" note above). Manually curated after a heuristic scan produced ~104 candidates, most of which were legitimate meaning-only radical glosses (e.g. `扌 (มือ)` = "hand", not a bug).
  - **Two more mistranslation patterns found (audit complete, fix drafted but *not yet applied* — see rationale below):** (a) "character" (a Chinese written character) mistranslated as `ตัวละคร` (a story/drama persona) instead of `ตัวอักษร`/`อักขระ` — 7 instances; (b) the character `面` (miàn) itself replaced by `เลดี้` (a garbled transliteration of the English word "lady") — 3 instances. `fix_character_mistranslations.py` exists locally with both fixes dry-run-verified, but **was deliberately not applied**: all 10 affected lessons (`hsk1_day11/15/21/22`, `hsk2_day1/4/6/7`) are still un-rolled-out, so per the "Operating principle" above, a `_th`-only patch there would be silently wiped the next time the rollout reaches them. Re-evaluate after each lesson's next rollout pass instead.
- ✅ **`CITEMARK` Placeholder Mistranslation Fixed (Aug 3):** Fixed in `generate_hsk_full.py` by switching to code-structured `__CIT_{n}__` placeholders and pre-compiled regex matching for legacy/transliterated tokens. Verified via `test_field_masking.py`.
- ✅ **Citation Punctuation & Instruction Prefix Validation Fix (Aug 4):** Refined `_extract_citations()` in `generate_hsk_full.py` to strip trailing CJK sentence punctuation (`。`, `，`, `！`, `？`) and prompt instruction prefixes (`填空：`, `选词填空：`, `填写空白`) from citation cores. This eliminated false-positive validation rejections for natural LLM translations, allowing `hsk1_day7` and `hsk1_day11` to pass cleanly. Added Test 7 (`test_citation_extraction_refinements`) to `test_field_masking.py` (all 7 tests pass 100%).
- ✅ **HSK1 Thai Translation Rollout Progress (Aug 5):** Resumed rollout; upgraded 10 new lessons (`hsk1_day8`, `day12`, `day15`, `day16`, `day17`, `day18`, `day19`, `day20`, `day21`, `day23`) to `"llm"` quality in `patched_lessons_state.json` and Turso DB. Total HSK1 lessons locked on LLM Thai reached **22 lessons** (out of 26). `hsk1_day8` and `hsk1_day12` were successfully rescued from fallback status. Verified `QuotaExceededError` cleanly stops execution when hitting daily Gemini free tier limit (20 req/day) at `hsk1_day25`.
- ✅ **Pinyin Rules (Lesson 0) Seeded into Turso DB (Aug 4):** Updated `insert_pinyin_rules.js` with bilingual (`_en`/`_th`) columns for 4 core Pinyin rules (The Four Tones, Third Tone Sandhi, Tone Change for Yi/Bu, and the 'ü' Rule) and seeded `hsk1_day0` into Turso DB using 0 LLM API quota.
- ✅ **Visual 5-Tab Pinyin Suite & Pedagogical Redesign (Aug 5):** Refactored Pinyin suite into 5 categorized tabs (`📊 Syllable Matrix`, `👄 Mouth & Airflow Guide`, `🎵 Tone & General Rules`, `🎧 Tone Pairs Practice`, `⌨️ Pinyin Typing Game`). Added Language Purity Guardrail in `insert_pinyin_rules.js` (0% EN-TH mixing), implemented 0ms memory caching in `app.js` (`state.pinyinLessonData`) for instant TH-EN toggles, and redesigned Pinyin Typing Simulator game loop (prompt -> type pinyin -> IME candidate pick -> Hanzi reveal + TTS).
- ✅ **Self-Hosted Open Pinyin Audio & Native Hanzi Fallback (Aug 5):** Self-hosted human MP3 audio files (`public/audio/pinyin/a1.mp3` - `a4.mp3`, `e1.mp3` - `e4.mp3`) directly inside the project repository to eliminate third-party commercial CDN dependencies and HTTP 404 failovers. Updated `playTone()` fallback to map ASCII Pinyin strings (`ā, á, ǎ, à`, `zhi1`) directly to Chinese Hanzi characters (`啊`, `知`, `吃`, `诗`, `日`), forcing browser SpeechSynthesis to activate native Mandarin voice engines instead of reading ASCII letters ("zhi-one").
- ✅ **Universal Web Speech API TTS Fallback for Pinyin Chart (Aug 4):** Updated `playTone()` in `app.js` with an `audio.onerror` listener and `.catch()` fallback routing through native Web Speech API `SpeechSynthesis` (`lang: 'zh-CN'`), resolving broken/missing audio for rare Pinyin syllables (e.g. `tei`) across the entire Pinyin chart.

**Pending / Next Steps:**
- ⏳ **HSK 2 Days 18–21:** Generate 4 remaining lessons (quota allows ~20 req/day; use `--no-translate` flag to save quota for separate patch pass).
  - Command: `python generate_hsk_full.py --limit 4 --no-translate`, then `node insert_generated_lessons.js`
- ⏳ **Thai Translation Rollout for HSK1 (Remaining 4 Days) & HSK2 (all 17 days):**
  - Resuming rollout once daily API quota resets: `python patch_thai_translations.py --level hsk1`. Currently 22 HSK1 lessons are locked on `"llm"`. 2 fallback (`day22`, `day24`) + 2 untouched (`day25`, `day26`) HSK1 lessons + 17 HSK2 lessons remain for future quota runs.
  - **Before running:** check `patched_lessons_state.json` to see what's already locked — do not re-run without `--limit`/`--id` scoping unless you intend to process the whole remaining level in one go (the script itself skips `"llm"`-locked lessons automatically, but a full unscoped run could burn the whole day's quota on the first several lessons if some hit repeated fallback retries).
  - **What to watch for:** if `hsk1_day1/7/8` (currently `"fallback"`) get reprocessed and *still* fail, that's a signal the Aug 2 citation-validation fix didn't fully resolve their specific failure mode — worth re-diffing their actual LLM response against `find_translation_corruption()`'s checks rather than assuming it's just model variance.
  - **If quota runs out mid-run:** this is now handled safely and automatically (see "Thai Translation Pipeline" subsection above) — the script prints `[QUOTA EXCEEDED]`, stops cleanly, and leaves the interrupted lesson + everything after it untouched. Just re-run the same command after quota resets; no manual cleanup needed.
- ⏳ **Thai Translation Rollout for HSK2 (all 17 days) — After HSK1 completes:**
  - `patched_lessons_state.json` currently has zero HSK2 entries — none of the 17 HSK2 lessons have ever been through the rollout. Same command pattern: `python patch_thai_translations.py --level hsk2 --limit N`.
  - Once both HSK1 and HSK2 have been through at least one rollout pass, re-run the "content-quality audit" methodology from Aug 2 (search all `_th` fields for homograph-risk English terms, cross-reference vocab citations against known pinyin, diff DB against JSONL) — the Aug 2 audit found real bugs (character/lady mistranslation, pinyin transliteration) that are likely to recur or be newly introduced by fresh LLM translations, so a fresh audit pass afterward is worth the time, not just a one-off.
  - **Do not hand-patch `_th`-only content on any lesson before its rollout pass** — see the "Operating principle" note above. The `fix_character_mistranslations.py` script (10 confirmed bugs, dry-run verified) is sitting ready to apply, but should only be run *after* checking whether each target lesson is still exhibiting the bug post-rollout — the rollout's own contextual LLM translation may well fix it without any manual patch needed.
- ✅ **CITEMARK Placeholder Mistranslation Fixed (Aug 3):** Fixed in `generate_hsk_full.py` by switching to code-structured `__CIT_{n}__` placeholders and pre-compiled regex matching for legacy/transliterated tokens. Verified via `test_field_masking.py`.
- ⏳ **Deferred from Aug 2 — Kid-Unfriendly Grammar Explanation Tone:** User flagged that `hsk1_day5`'s grammar explanation (and likely many others) reads like a linguistics reference (formal grammar-slot notation, "in more complex numbers") rather than something an 8-15 year old would find approachable, in both English and Thai. This was explicitly deferred as a separate, larger effort — likely a generation-prompt rewrite (simpler tone instructions) plus a batch re-translation pass, which is quota-limited like everything else here. Not started.
  - `memory/deployment_quota_strategy.md` (this assistant's own memory file, not a repo file) was refreshed Aug 2 with the real, confirmed 20/day quota figure and current rollout progress — consult it for day-by-day budget planning, though note it reflects a snapshot in time and should be re-verified against `patched_lessons_state.json` before trusting exact counts.
- ⏳ **HSK 3 Seeding:** Content generation and seeding for 500-word intermediate-advanced curriculum.
- ⏳ **HSK 4, 5 & 6 Seeding:** Advanced content generation and database seeding for higher proficiency levels.
- ⏳ **Phase 6: Future Enhancements:**
  - *Multi-Character Writing Pad:* Expanding the `hanzi-writer` integration to support writing multiple characters sequentially for vocabularies longer than one character.
  - *Full HSK Mock Exams:* Adding comprehensive end-of-level exams that accurately simulate the latest official HSK exam formats (2026 version) for formal test preparation.
  - *Pronunciation Assessment:* Integrating a speech-to-text API (e.g., Web Speech API or external service) to score user pronunciation accuracy.
  - *DevOps & Custom Domains:* Docker containerization, formal production deployment on Vercel, and configuration of a clean, branded custom domain (avoiding phishing-like URLs).
  - *Automated Testing:* Implementing a test suite (Jest/Supertest) to guard against regressions in the API and UI state.

---

## 4. Key Discussions & Decisions Log

- **Interactive Confirmations Guardrail (Reverted TTY Check):** Decided to completely block force overwrites (`--force`) in non-interactive tasks. Reverted the seeder's automatic override patch to ensure force database deletes cannot run silently in background task runners, and locked this constraint locally in `.agents/AGENTS.md`.
- **CEDICT Parsing & Dictionary Lookup:** Moved vocabulary English definitions out of Gemini's scope by querying the Youdao API locally. This prevents Chinese characters from ending up in the English meaning fields.
- **LLM Contextual Thai Translation (July 27 — Replaced Google Translate):** Replaced the per-field Google Translate approach with a single `gemini-3.5-flash` LLM call per lesson. The full lesson JSON is submitted as context with a system prompt instructing natural, idiomatic, kid-friendly Thai. This eliminates the word-by-word translation bug that produced broken `deconstruct_th` and `explanation_th` content. A `--no-translate` CLI flag was added to `generate_hsk_full.py` to allow quota-efficient two-pass generation (generate first, translate separately).
- **Turso Thai Patch Strategy (No Re-seeding):** HSK1 Thai translation quality fix will be applied via targeted `UPDATE` SQL using `patch_thai_translations.py`. This avoids destructive re-seeding and respects the project rule against non-interactive force overwrites.
- **google.genai SDK Migration (July 27):** Migrated from the deprecated `google-generativeai` package (which caused silent 16-hour hangs with `gemini-3.5-flash`) to the current `google.genai` SDK. `gemini-3.5-flash` confirmed working; `gemini-2.0-flash` and `gemini-1.5-flash` are either quota-exhausted or not available on the free API version.
- **Free Tier Quota Constraint:** Both `gemini-3.5-flash` and `gemini-2.5-flash` share a **20 requests/day free tier limit**. With LLM translation costing 1 extra call per lesson, the practical generation rate is ~10 lessons/day (with translation) or ~20 lessons/day (with `--no-translate`).
- **Language Suffix Suffixing vs Unified Columns:** Unified character and Pinyin columns while splitting translation meanings (`meaning_en` / `meaning_th`) to avoid corruption, maintaining mixed-language compatibility in the database.
- **Auto-Language Detection in Translation Pipeline:** Configured the post-generation translator to use `sl=auto` rather than hardcoding `sl=en`.
- **Unique Character Constraint Handling (Polyphonic Words):** When encountering polyphonic words (same character with different pinyins/meanings like `好` as `hǎo` and `hào`) in the same day's "Additional Vocabulary" catch-all pool, they are programmatically distributed to different days to prevent database unique constraint conflicts.
- **Bilingual Title Prevention:** Added prompt constraints to the Gemini generator templates to enforce English-only titles and prohibit bilingual prefixes, letting the frontend handle structural formatting.
- **HSK Level Prefix Rendering Fix (July 27):** Updated `app.js` (lines 742, 795) to use a generic regex `l.id.replace(/^hsk\d_day/, '')` replacing the hardcoded `'hsk1_day'` string, so the dashboard correctly renders day numbers for HSK 2 and beyond.
- **Thai-Contamination Root Cause Correction (July 28):** An initial hypothesis attributed Thai text appearing in English fields (`title`, `deconstruct`) to `add_thai_translations_to_lesson()` overwriting the wrong dictionary keys. Direct inspection of the function and live data disproved this — every assignment in it correctly targets only `_th`-suffixed keys. The real cause was upstream: `generate_lesson_content()`'s prompt only enforced "English only" on the `title` field, and validation never checked a field's actual *language*, only key presence. Fixed by extending the English-only instruction to `deconstruct`/`meaning`/`explanation` and adding `find_thai_contamination()` to the validation block.
- **Grammar-Quiz "Incomplete Sentence" Root Cause Correction (July 28):** A second hypothesis attributed a reported "missing sentence" bug in the grammar quiz to a frontend template/rendering mismatch in `app.js`. Investigation showed `renderGrammarPane()` renders `practice.prompt` correctly as a plain string — the actual defect was in the data: several `grammar_practice` records had a generic instruction with no embedded example sentence, an empty `answer` array, or an `answer` that didn't match any token in `words` (making the exercise unwinnable via the drag-into-blank UI). Fixed via `find_incomplete_practice()` validation plus a one-time repair of all 16 affected records.
- **Full-Table CJK Integrity Audit (July 28):** Prompted by a request to verify a Chinese-protection guardrail existed in the translation pipeline, audited all 85 `grammar_practice` records by comparing the actual Chinese characters in `prompt_en` vs `prompt_th`. Found the pre-guardrail `translate_en_to_th()` had silently corrupted Thai translations across most of HSK2 (days 1–17) — dropping embedded Chinese sentences, mistranslating citation characters (`两`→`二`, `会`/`能`→`will`/`can`), and producing `NULL` in 5 cases. This reinforced a standing practice for this class of bug: always diff actual before/after content against a hypothesis rather than trusting it, since two prior hypotheses in this same investigation (translation key-mapping, frontend rendering) were both wrong on inspection.
- **Placeholder-Substitution Translation Strategy (July 28):** Segment-splitting text into protected/translatable chunks and translating each chunk independently (the first guardrail approach) proved unreliable — Google Translate trims whitespace from short isolated fragments and breaks grammatical continuity when a sentence is chopped around a citation (e.g. "由 X 和 Y 组成" fragmenting into disconnected words like "Depend on X and Y Composition"). Switched to placeholder substitution instead: replace protected spans with a marker token, translate the whole string as one coherent unit, then restore the original spans afterward. This produced fluent, correctly-spaced output and is now used by both `translate_en_to_th()` and the standalone Chinese→English repair script.
- **Field Masking for JSON Structure Preservation (July 29):** hsk1_day4 retry failed validation with "vocab[你]: character/pinyin field altered" and "grammar[...].examples: cn/py field altered" despite explicit LLM prompt instructions "NEVER modify 'character', 'pinyin', 'cn', or 'py' fields." Root cause analysis revealed the pattern-based protection (`_CJK_PROTECT_PATTERN`) designed to protect citations *within text* has zero effect on JSON *field names or values* — the LLM still receives raw `"cn": "..."` and `"py": "..."` keys and can modify them. Attempted solution of stricter prompt instructions proved insufficient; LLM instruction-following is probabilistic, not guaranteed. Implemented structural field masking instead: rename untranslatable fields (`character`/`pinyin`/`cn`/`py` → `_*_masked`) before sending to the LLM, then unmask the response to restore original field names. Unit test suite (`test_field_masking.py`) validated the round-trip logic against a hand-written, perfectly-behaved fake response. **This was superseded July 31 — see below; the round-trip was never actually necessary.**
- **Field Masking Root-Cause Correction — Strip-Before-Send (July 31):** The July 29 masking fix was believed "structurally airtight" but a live retry of hsk1_day4 still failed with the exact same `character/pinyin field altered` error. Investigation found two compounding mistakes: (1) `_apply_translated_fields()`, the function that merges the LLM's response into the lesson, never reads `character`/`pinyin`/`cn`/`py` from the translated object at all — the byte-for-byte comparison was guarding a value nothing downstream consumes, so no live API call was ever actually needed to validate it. (2) Renaming a field to an unusual synthetic name (`_character_masked`) and asking the LLM to echo it back exactly is not more reliable than asking it to leave a common field name alone — it relocates the same instruction-following risk rather than removing it. Replaced the mask/unmask round trip with `_strip_untranslatable_fields()`, which deletes these fields from the LLM payload before sending and never expects them back; removed the corresponding equality checks from `find_translation_corruption()`. Logged as retrospective lesson #19: trace whether a downstream consumer actually needs a value *before* spending live API credit to validate round-trip behavior, and unit tests for LLM-adjacent logic must include an adversarial/imperfect fixture, not just a hand-crafted ideal response.
- **Windows Console Encoding Crash (July 31):** A live retry surfaced a second, unrelated bug: printing a validation-error message containing Chinese/Thai characters raised `UnicodeEncodeError` under Windows' default `cp1252` console encoding, killing the retry loop after attempt 1 instead of exhausting all `max_retries` and falling back gracefully as designed. Fixed by reconfiguring `sys.stdout`/`sys.stderr` to UTF-8 at the top of `generate_hsk_full.py`. This may explain why some earlier retry attempts appeared to fail outright rather than completing their full retry budget.
- **JSON-Parsing Fragility (July 31 — Open, User Investigating `json_repair`):** With the masking and encoding bugs both fixed, a further live retry of hsk1_day4 ran all 3 retries (confirming the crash fix) but every attempt failed at `json.loads()` — `Extra data: line 2 column 1` (trailing content after an otherwise-complete JSON object) and `Expecting ',' delimiter: line 238 column 27` (likely an unescaped `"` inside a string value, plausibly from this lesson's grammar title `Expressing Possession with "的" (de)`). Two fix paths were scoped: (a) defensive brace-matched JSON extraction plus a targeted repair pass, kept minimal and offline-testable; or (b) switching Gemini's config from a loose `response_mime_type="application/json"` to a strict `response_schema` (constrained decoding), which is more robust but requires modeling the full nested lesson shape and can only be validated live. Decision: the user will first try the third-party `json_repair` library independently before either is implemented here.
- **JSON-Parsing Fragility Resolved with `json_repair` (Aug 2):** User independently confirmed the `json_repair` library successfully parsed both of the malformed payloads that had blocked `hsk1_day4`. Implemented the fallback in both generation call sites (see Milestones above) rather than the more involved `response_schema` approach that had been scoped as the alternative — simpler, offline-testable, and the user's own local test had already validated the library works on the exact failure shapes seen live. Confirmed working on a subsequent real API call the same day (`hsk1_day2`'s malformed second attempt was salvaged).
- **Turso Websocket Transport Root Cause (Aug 2):** `patch_thai_translations.py`'s DB writes were failing with a generic `400, message='Invalid response status'` error that gave no indication of the real cause. Initial hypotheses (auth token issue, leaked connections from the not-yet-found `db.close()` bug) were both ruled out by direct testing (a plain HTTPS GET with no auth correctly returned a `401` with a clear body, proving the endpoint was reachable and the auth stack worked normally) before landing on the actual cause: manually performing the raw WebSocket handshake and reading the response body (something `aiohttp`'s exception object doesn't expose by default) revealed the literal server response `{"error":"protocol upgrade not supported (websocket)"}` — this specific Turso database instance doesn't support the Hrana-over-WebSocket transport that `libsql://`/`wss://` URLs default to, regardless of subprotocol requested. This reinforces the project's standing practice (see July 28 CJK Integrity Audit entry) of diffing actual behavior rather than trusting a plausible-sounding hypothesis.
- **Quota-Exceeded Detection Design — Fail-Stop Instead of Fail-Degrade (Aug 2):** Considered simply lowering `max_retries` or shortening backoff sleeps to reduce wasted quota on failure, but this doesn't distinguish *why* a lesson is failing — a quota error and a content/JSON error look identical to a generic `except Exception`. Instead, specifically detect `google.genai.errors.APIError` with `.code == 429` and, after one 60s-backoff retry (to tolerate a transient per-minute rate limit rather than assuming immediate full quota exhaustion), raise a dedicated `QuotaExceededError` that the batch loop catches to stop the *entire run* immediately — rather than let quota exhaustion masquerade as a content problem and silently degrade every remaining lesson in the batch to the Google Translate fallback. Verified live against the real API (see Milestones above): the interrupted lesson's on-disk state was confirmed completely unaffected.
- **Citation Validation Over-Strictness — Root Cause and Fix Design (Aug 2):** `hsk1_day1/7/8` kept failing LLM validation with citation-dropped errors even though the LLM's translations were plausibly natural. Rather than assume model flakiness, diffed the actual failing citations against the validator's logic and found `_extract_citations()` required an entire `character (pinyin, gloss)` parenthetical — English gloss words included — to survive untranslated, which is an unreasonable ask of a "natural, kid-friendly Thai translation." Considered simply loosening the citation regex's length cap first, but recognized (prompted by a user question) that the length cap is shared between two different consumers with different needs: the validator (`_extract_citations`) and the Google-Translate fallback's placeholder protection (`translate_en_to_th`) — raising the cap alone would have fixed the validator but made the fallback path freeze *more* (longer) gloss content in English, the opposite of the intended improvement. Resolved by adding one shared helper, `_split_citation()`, that separates "character + pinyin" (must survive) from "free-text gloss" (safe, and preferable, to translate) — used by both consumers so each does the *correct*, not just *consistent*, thing with the split. Confirmed via the user's specific request to test against real content, not just synthetic examples, which surfaced two further pre-existing gaps in the same code path (hyphen-separated citations losing pinyin protection entirely, and a too-tight length cap dropping protection for longer glosses) — both fixed in the same pass since they shared the same root architecture.
- **Deferred Hand-Patching of Un-Rolled-Out Lessons (Aug 2):** After preparing a 10-record fix for the "character"/"lady" mistranslation bugs, realized (again prompted by a user question, this time about interaction with the *planned* rollout) that `patch_thai_translations.py` fully clears and regenerates every `_th` field for any lesson not yet locked `"llm"` — so a Thai-only patch to a still-pending lesson is silently overwritten the next time the rollout reaches it, regardless of whether that attempt succeeds via LLM or falls back again. Decided to hold all Thai-only fixes for lessons not yet rollout-locked, re-auditing after each lesson's rollout pass instead of patching twice. This reframes "hand-patch content bugs found via audit" as useful *only* for either (a) English/Chinese source corrections (which the rollout never touches and which also improve future translation attempts) or (b) lessons already permanently locked.
- **`CITEMARK` Placeholder Mistranslation — Documented, Deliberately Not Fixed (Aug 2):** While explaining the placeholder-substitution mechanism to the user, traced a live example of it failing (`hsk1_day5`'s "Additional Vocabulary" grammar practice `prompt_th` ending in the garbled `ไซท์มาร์ก0` instead of the intended Chinese question) to Google Translate occasionally transliterating the literal `CITEMARK0` marker into Thai script instead of leaving it untouched, which breaks the exact-string-match restore step. Decided not to fix this in the same session: it's orthogonal to the citation-validation fix (affects only the Google-Translate fallback path, never the LLM path), and the priority for this project right now is reducing *reliance* on the fallback path (via the rollout) rather than perfecting its output quality. Documented clearly for a future session instead of leaving it as an offhand remark.
- **Vocab Garden SRS & Automatic System Grading (Aug 8):** Designed and deployed a kid-friendly SRS memory garden. Eliminated confusing 4-button Anki self-grading menus in favor of automatic system evaluation (`MISSED`, `GOT_IT`, `PERFECT`). Integrated UTC+7 (Asia/Bangkok) date calculation protection, historical progress backfilling, Wilting Rescue Box (`times_forgotten >= 3`), and full-screen `#srs-view` review arena. Verified live on Turso Cloud DB and deployed to production.
- **Vocab Garden Upgrade: Decoupled & Game-based Retention Engine (Aug 10):** Successfully refactored frontend into ES modules, overhauled the main dashboard to center-stage the memory garden canvas, and implemented active cognitive challenges. Added **Seed Fusion Lab** (interactive drag/drop and backend validation `/api/srs/fuse` to plant compound words) and **Sentence Quest Builder** (constructs sentences dynamically using mastered Stage 4 words).
- **HSK 3 Curriculum Seeding (Aug 10):** Seeded HSK 3 Days 1, 2, and 3 vocabulary lessons into the live database.

---

## 4. Retrospective Lessons Learned

### Lesson #20: Vercel server startup crash diagnostic
- **Issue:** During an automated code editing session, the server code was modified and saved without a critical closing block (`});`) inside the `app.post('/api/progress')` route handler in [server.js](file:///C:/Users/USER/OneDrive/Desktop/knowledge/My%20project/Chinese%20web%20learning/server.js). This caused Vercel serverless functions to crash at startup with `SyntaxError: Unexpected end of input`.
- **Diagnostic Lesson:** Never push a javascript or node.js update to remote main/production without first verifying its syntax locally using parsing utilities (such as `node -c server.js` or parsing checkers). Pre-commit scripts and continuous integration syntax gates are highly recommended for solo/small team deployments to catch structural defects.


## 5. Historical Note: "Supporting Documentation for Tomorrow" (Written July 29, superseded July 31)

The five documents below were written July 29 as a same-day action plan for a "tomorrow" that has since passed. They describe the field-masking approach that was later found unnecessary and replaced (see Decision Log above) — they're kept for historical trace, not as current guidance:
1. `TOMORROW_ACTION_PLAN.md`, `MASKING_IMPLEMENTATION_STATUS.md`, `PATTERN_ANALYSIS_DAY4.md` — describe and analyze the now-superseded masking approach.
2. `memory/deployment_quota_strategy.md` (this assistant's own memory file, not a repo file) — **refreshed Aug 2** with the real confirmed 20/day quota, current rollout progress, and an updated per-day lesson-budget estimate now that the masking-era assumptions no longer apply. Consult it directly for current numbers rather than this historical note.
3. `test_field_masking.py` — rewritten July 31 to test the strip-based approach instead; no longer tests mask/unmask.

**Update (Aug 2): the JSON-parsing blocker noted above is now fixed** (`json_repair` fallback, confirmed working live). The rollout has resumed — see Section 3's Aug 2 milestones and Pending/Next Steps for current status and exact resume commands.
